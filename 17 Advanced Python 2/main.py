# VIRTUAL ENVIRONMENT => isolated environment or different environment from actual or main environment

# command to activate virtual environment in windows powershell:
# .\env\Scripts\activate.ps1

# 09:06:36

# pip freeze => returns the installed packages in the current (global or virtual) environment
# pip freeze > requirements.txt => saves the installed packages in the current environment to a file named requirements.txt
# pip install -r requirements.txt => installs the packages listed in the requirements.txt file to the current environment